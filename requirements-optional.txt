# The HRRR NWP dependencies (herbie-data, xarray, cfgrib, eccodes) moved into requirements.txt
# when the real HRRRProvider was implemented — NWP_PROVIDER=hrrr now works from a plain
# `pip install -r requirements.txt`. Nothing optional remains.
